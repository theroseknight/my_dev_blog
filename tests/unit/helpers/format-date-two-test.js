import {
  formatDateTwo
} from '../../../helpers/format-date-two';
import { module, test } from 'qunit';

module('FormatDateTwoHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatDateTwo(42);
  assert.ok(result);
});
